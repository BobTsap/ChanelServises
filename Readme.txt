# Приложение 1 раз в минуту считывает данные из Google таблицы,
# Создает на основе данных таблицу в БД
# Расчитываются данные для новой колонки, используя данные по курсу доллара на данный момент
# 
# Перед запуском приложения необходимо:
# 1. Установить пакеты из req.txt командой в консоли: pip install -r req.txt
# 2. Создать БД на локальном сервере set_table() и прописать для неё свои: host, user, password, database 
# (Так как БД создается для примера локально)
#
# Запустить скрипт main.py на выполнение
# Для остановки: в консоли Ctrl+Ctrl
